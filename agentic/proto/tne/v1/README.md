# TNE proto extensions (reserved)

Reserved for any TNE-specific protos that extend ARTF via the
`extensions 500 to max` range on `RTBRequest.Ext`. Phase 1 has none —
this directory is a placeholder so the vendoring layout is stable.
